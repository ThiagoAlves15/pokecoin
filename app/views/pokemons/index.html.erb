<div class="cointainer d-flex flex-wrap align-items-center justify-content-center m-5">
  <div class="col-md-8">
    <h2 class="mb-4">
      Pokemons
    </h2>

    <div class="mb-4">
      <%= link_to 'New pokemon', new_pokemon_path, class: "btn btn-outline-primary" %>

      <% @pokemons.each do |pokemon| %>
        <%= render pokemon %>
      <% end %>
    </div>
  </div>
</div>
