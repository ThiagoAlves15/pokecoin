<nav class="navbar navbar-expand-lg sticky-top navbar-dark bg-dark">
  <div class="container-fluid">
    <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarSupportedContent" aria-controls="navbarSupportedContent" aria-expanded="false" aria-label="Toggle navigation">
      <span class="navbar-toggler-icon"></span>
    </button>

    <%= link_to 'Mind Hander', root_path, class: "navbar-brand" %>

    <%= render 'layouts/avatar', type: "mobile" %>

    <div class="collapse navbar-collapse" id="navbarSupportedContent">
      <ul class="navbar-nav me-auto">
        <li class="nav-item mt-2 mb-2">
          <%= link_to I18n.t('activerecord.models.privacy_policy.one'), privacy_policy_path, class: "nav-link btn btn-link" %>
        </li>

        <li class="nav-item mt-2 mb-2">
          <%= link_to I18n.t('activerecord.models.product.other'), products_path, class: "nav-link btn btn-link" %>
        </li>

        <li class="nav-item mt-2 mb-2">
          <%= link_to "Blog", posts_path, class: "nav-link btn btn-link" %>
        </li>

        <% if user_signed_in? %>
          <li class="nav-item mt-2 mb-2">
            <%= link_to I18n.t('activerecord.models.service_order.other'), service_orders_path, class: "nav-link btn btn-link" %>
          </li>
        <% end %>
      </ul>
    </div>

    <%= render 'layouts/avatar', type: "desktop" %>

    <div class="header__dropdown">
      <ul class="dropdown-menu text-small" data-popper-placement="bottom-end">
        <% if user_signed_in? %>
          <li>
            <%= link_to I18n.t('devise.shared.links.profile'), edit_user_registration_path, class: "dropdown-item" %>
          </li>
          <li>
            <%= link_to I18n.t('devise.shared.links.service_orders'), service_orders_path, class: "dropdown-item" %>
          </li>
          <li><hr class="dropdown-divider"></li>
          <li>
            <%= button_to I18n.t('devise.shared.links.sign_out'), destroy_user_session_path, method: :delete, class: "dropdown-item" %>
          </li>
        <% else %>
          <li>
            <%= link_to I18n.t('devise.shared.links.sign_in'), new_user_session_path, class: "dropdown-item" %>
          </li>
        <% end %>
      </ul>
    </div>
  </div>
</nav>
